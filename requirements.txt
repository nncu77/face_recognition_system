# Core
numpy==1.24.3
opencv-python==4.9.0.80
Pillow==10.2.0

# Face Recognition (商用 OK — 但請查 InsightFace model_zoo 確認 buffalo_l 訓練資料 license)
insightface==0.7.3
onnxruntime==1.17.0
# GPU 路線（要先裝 CUDA Toolkit 11.7 + cuDNN 8.x，onnxruntime-gpu 不內建這兩個）:
#   1. uninstall onnxruntime
#   2. CUDA 11.7 → pip install onnxruntime-gpu==1.15.1
#      CUDA 11.8 → pip install onnxruntime-gpu==1.17.0
#   3. settings.USE_GPU=True (or env USE_GPU=true)
# 即使 CUDA DLL 找不到也會 fallback CPU，不會炸

# MediaPipe for liveness detection
mediapipe==0.10.9

# Backend
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
pydantic==2.5.3
pydantic-settings==2.1.0   # config.py 用 BaseSettings 需要這個

# Database
sqlalchemy==2.0.25

# UI
streamlit==1.30.0
streamlit-webrtc==0.47.1

# Utils
python-dotenv==1.0.0
loguru==0.7.2

# Test
pytest==7.4.4
httpx==0.26.0
